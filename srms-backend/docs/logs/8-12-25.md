## 2025-12-08 – SRMS Daily Log

### Date
- **08/12/2025**

### Tasks Completed
- **GitHub repository & project setup**
  - Created the `srms` mono-repo on GitHub with initial `README.md` and project description.
  - Linked it with a GitHub board for tracking tasks and progress.
- **Database design and ERD**
  - Designed core entities: `users`, `roles`, `permissions`, `role_has_permissions`, `services`, `service_requests`, `comments`, `media`, `activity_logs`.
  - Defined primary keys, main relationships, and an initial set of attributes for each table.
- **Monorepo structure**
  - Created a structured mono-repo with:
    - `srms-backend` (Laravel API),
    - `srms-front-end` (React SPA),
    - `srms-node-js` (Node.js microservice).
- **Laravel base setup**
  - Initialized a new Laravel project in `srms-backend`.
  - Installed base dependencies and confirmed the application scaffold is ready for further configuration.

### Tasks Pending
- **None** for this day.

### Blockers
- **None** encountered.

### Learnings for the Day
- **GitHub & project initialization**
  - Understood how to bootstrap a clean repository with a clear README and how that supports future collaboration and reviews.
  - Saw how a GitHub Project/board helps convert high-level goals (PIP) into trackable tasks.
- **Database & ERD thinking**
  - Practiced turning business requirements (service requests, roles, comments, attachments) into tables, relationships, and attributes.
  - Noticed the importance of planning polymorphic tables (`comments`, `media`, `activity_logs`) early for extensibility.
- **Monorepo structure**
  - Learned how grouping backend, frontend, and microservices under one repo simplifies shared documentation and consistent standards.
- **Laravel setup**
  - Reinforced the steps involved in initializing a modern Laravel app (composer install, config, base structure), preparing for strict standards (PSR-12, Pest, etc.).

### Mistakes & Fixes
- **No major mistakes recorded today**
  - Any potential enhancements (e.g. indexes, enums, extra audit fields) are noted for future iterations of the ERD.

### Best Practices Applied
- **Planning before coding**
  - Invested time in ERD and repository structure before writing business logic.
- **Documentation early**
  - Started documenting from day one (README, ERD) to keep future work aligned with the design.